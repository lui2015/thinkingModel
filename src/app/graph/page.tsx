import GraphClient from "@/components/GraphClient";

export const metadata = {
  title: "模型图谱",
  description: "可视化探索思维模型之间的关联关系",
};

export default function GraphPage() {
  return <GraphClient />;
}
